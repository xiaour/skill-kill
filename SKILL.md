---
name: "skill-kill"
description: "又名“必杀技”，帮你查找、比较并评估合适的 Skill，披露来源、信任度和安全风险。为你的AI应用查找靠谱的Skill。"
---

# Skill Kill

## 目标

当用户描述了想要的业务能力、技术目标或工作流，却不知道应该安装哪个 Skill 时，从多个 Skill 市场发现候选项，比较功能匹配度，并披露来源、信任度和安全风险。

## 触发条件

在以下场景调用本 Skill：

- 用户询问“有没有 Skill 可以……”或“不知道应该安装什么 Skill”。
- 用户希望从 skillhub.cn、Skill 市场、插件目录、Git 仓库或官方注册源寻找能力扩展。
- 用户希望比较多个 Skill 的功能、维护状态、权限需求或安全性。
- 用户要求判断某个候选 Skill 是否值得安装。

如果用户要求创建、修改或调试 Skill 本身，不要将其误判为 Skill 发现请求，应使用对应流程。

## 检索原则

1. 先拆解用户需求：目标、输入、输出、运行环境、必须能力、可接受权限和限制条件。需求确实不明确时，最多询问 3 个关键问题。
2. 默认检索官方注册源、维护方/平台插件目录、公开代码仓库和社区市场，并区分来源类型。
3. 只报告实际检索到的事实；不得编造作者、评分、安装量、审计结果、权限、漏洞、版本或更新时间。
4. 优先选择功能匹配度高、来源明确、权限最小、有公开源码或文档、维护状态清晰的候选项。
5. 对同一个 Skill 的镜像、分发页和源码页去重合并。
6. 没有足够可靠的候选时，明确说明未找到可验证的合适 Skill，不要为了凑数推荐低可信结果。

## 安全与信任评估

对每个候选单独评估，不能用市场整体信誉代替具体评估。

### 信任度

只允许使用 `高 / 中 / 低 / 未知`：

- **高**：官方或明确维护方来源，作者可验证，有公开源码/文档，版本、维护记录和权限说明清晰。
- **中**：来源或维护者基本可验证，但源码、维护活跃度、权限说明或独立反馈存在缺口。
- **低**：身份、维护、源码、发布流程或权限信息不足，或存在明显异常。
- **未知**：证据不足；未知不等于安全。

### 安全风险

只允许使用 `低 / 中 / 高 / 未知`。检查安装脚本、运行时代码、外部网络访问、文件读写、命令执行、凭据/环境变量访问、代码下载、动态执行和实际权限范围。

重点提醒读取密钥、隐蔽外传、修改系统、执行任意命令和访问私有资源等行为。无法查看源码或完成检查时必须标记为 `未知`，不得推断为低风险。

发现有证据支持的凭据窃取、隐蔽外传、任意命令执行或来源伪造时，标记为高风险并停止推荐。

### 来源披露

每个候选至少列出来源类型、作者/组织（若可验证）、候选页面 URL、源码 URL（若有）、版本/更新时间（若能核验）、检查时间和证据限制。URL 必须来自实际检索结果。

## 固定化处理流程

为避免输出随机，严格按以下顺序执行：

1. 先生成候选 JSON 数据，不直接生成自然语言结果。
2. 每个候选必须符合 `candidate.schema.json` 的字段和枚举约束，不得增删字段；缺失信息填写 `未知`。
3. 使用 `renderer.js` 的 `prepareCandidates()` 规范化、排序并截取最多 5 个候选。
4. 使用 `renderer.js` 的 `renderCandidates()` 渲染结果。未指定格式时使用 `markdown`，用户明确要求 HTML 时使用 `html`。
5. 输出顺序固定为：结论、候选列表、来源与证据、安装前检查、替代方案。
6. 脚本不可执行时，严格按照相同字段、排序规则和输出顺序手动处理。

### 外部文件

- [candidate.schema.json](./candidate.schema.json)：候选 Skill 的固定 JSON Schema，定义字段、类型和允许枚举。
- [renderer.js](./renderer.js)：候选规范化、排序、最多 5 条截取和 Markdown/HTML 渲染入口。

不要在 `SKILL.md` 中复制或改写这两个文件的实现；修改格式时优先更新外部文件，并同步检查本节约定。

## 默认输出格式

默认最多输出 5 个候选 Skill；候选不足时可以少于 5 个。

默认使用 Markdown。候选表固定包含：候选 Skill、匹配度、信任度、安全风险、来源、关键权限/行为和推荐结论。每个候选后必须有“复制给 AI 安装”操作。

HTML 模式最多输出 5 个 `.skill-result` 卡片。每张卡片必须包含候选名称、匹配度、信任度、安全风险、来源、关键权限/行为、证据限制和一个按钮。按钮使用 `data-install-prompt` 保存完整安装指令，并对属性值进行 HTML 转义。

安装指令必须包含 Skill 名称和实际来源 URL，并要求安装前再次核验来源、源码、版本、依赖、权限和安全风险。按钮只负责复制，不得自动安装、启用、运行或授权。

固定输出顺序：

1. **结论**
2. **候选列表**
3. **来源与证据**
4. **安装前检查**
5. **替代方案**

明确区分 `事实`、`推断` 和 `未知`。对时效性信息注明检索日期。

## 交互与边界

- 本 Skill 负责发现、比较和风险提示；除非用户明确确认，不自动安装、启用、运行或授权任何候选 Skill。
- 用户要求直接安装时，先展示候选、来源、信任度、安全风险和所需权限，并请求确认具体候选。
- 不提供“绝对安全”“官方背书”或“无风险”等保证。
- 不将用户密钥、令牌、私有路径或敏感需求发送到外部搜索；只使用最小必要信息。
- 安装按钮中的指令不得包含用户密钥、令牌或其他敏感信息。
